name: Build Flutter APK (Release)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: main_repo

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'

      - name: Install dependencies
        run: flutter pub get
        working-directory: main_repo/ps4_lounge

      - name: Build APK
        run: flutter build apk --release
        working-directory: main_repo/ps4_lounge

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: main_repo/ps4_lounge/build/app/outputs/flutter-apk/app-release.apk